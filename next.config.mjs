/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['ap-south-1.graphassets.com'], // Add your image source domain here
  },
};

export default nextConfig;
